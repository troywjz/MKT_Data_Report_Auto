# 项目记忆 - 营销数据报表自动化

> 公开脱敏版。本文保留项目背景、架构和维护经验，不包含真实账号、密码、Token、客户域名、账户名、业务数据或内部人员信息。

## 项目名称

营销数据报表自动化工具

## 项目概述

本项目用于自动化生成市场投放日报和时报。系统通过浏览器自动化抓取广告平台数据，通过 CRM 导出线索数据，再根据 Excel 模板汇总出可用于业务沟通的日报、时报和发送模板。

项目重点包括：

1. 多平台广告数据采集。
2. CRM 线索数据读取与匹配。
3. Excel 模板化处理。
4. 时报 Markdown 表格生成。
5. DingTalk 机器人发送能力。
6. 本地账号、浏览器登录态、Webhook 等敏感信息隔离。

## 主要模块

### 入口脚本

`main.py` 是项目主入口，支持两种模式：

```bash
python main.py shibao
python main.py ribao
python main.py ribao 2026-04-01
```

### 数据采集

`spider/` 目录包含广告平台和 CRM 数据采集逻辑。采集脚本使用 DrissionPage 连接 Chromium，并通过 `.env` 或本地配置获取账号、密码、URL、浏览器用户数据目录和调试端口。

### Excel 处理

`excelproc/` 目录负责日报、时报模板处理、字段映射、线索匹配和汇总写入。

### 本地配置

`app_config.py` 负责读取 `.env`。公开仓库只提供 `.env.example`，真实 `.env` 不提交。

### 自动化 Skill

`skills/` 目录保留公开脱敏后的操作说明，用于指导自动化代理运行日报和时报流程。所有 DingTalk、账号、密码和 URL 均要求通过环境变量读取。

## 敏感信息处理原则

1. `.env`、`config.xlsx`、浏览器用户数据目录、真实业务 Excel、JSON 输出和下载数据均不提交 Git。
2. `skills/` 文档不得出现真实 access token、appSecret、Webhook URL、账号、密码或内部域名。
3. README 和示例 Excel 只使用占位数据。
4. 若敏感信息曾被提交到远程仓库，应立即吊销相关 token 和密码，并清理 Git 历史。

## 当前开源准备状态

已完成：

1. 新增 `.gitignore`，忽略本地环境、真实数据、浏览器登录态和生成产物。
2. 新增 `.env.example`，说明需要配置的环境变量。
3. 新增 `app_config.py`，支持从 `.env` 读取配置。
4. 爬虫脚本已改为优先读取环境变量。
5. Skill 文档已脱敏。
6. 项目记忆已改为公开脱敏版。

待维护：

1. 持续检查新增文档和脚本中是否出现真实账号、URL、Token。
2. 保持示例 Excel 与实际模板字段结构同步。
3. 如新增平台，先扩展 `.env.example`，再接入代码。

## 运行经验

1. 浏览器登录态保存在本地用户数据目录中，不能提交。
2. Excel 文件被桌面程序打开时，脚本可能无法写入。
3. CRM 导出等待时间较长，应设置足够的超时。
4. 报表模板字段变化会影响匹配逻辑，调整模板后需要同步测试日报和时报。
5. 发送机器人失败时，优先检查 token、机器人权限、关键词限制和网络。
